const initialState = {
    isloading: false,
    isAuth: false,
    token: null,
    isError:false
}

export const reducer = (state = initialState, { type, payload }) => {
    switch (type) {
        default : return state
    }
}